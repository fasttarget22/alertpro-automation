name: Daily Smoke Shop Emails Real (DISABLED)
on:
  workflow_dispatch:  # Manual only - no longer automatic

jobs:
  send-emails:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Using Claude Code on PC instead."
